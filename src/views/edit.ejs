<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Produto</title>
</head>
<body>
  <h1>Editar Produto</h1>
  <form action="/produto/edit/<%=produto.id%>" method="POST">
    <!-- Campo Nome -->
    <label for="nome">Nome</label>
    <input type="text" id="nome" name="nome" value="<%= produto.nome %>" required>

    <!-- Campo Valor -->
    <label for="valor">Valor</label>
    <input type="number" id="valor" name="valor" step="0.01" value="<%= produto.valor %>" required>

    <!-- Campo Categoria -->
    <label for="categoria">Categoria</label>
    <select id="categoria" name="categoria" required>
      <option value="Hardware" <%= produto.categoria === "hardware" ? "selected" : "" %>>Hardware</option>
      <option value="Software" <%= produto.categoria === "software" ? "selected" : "" %>>Software</option>
      <option value="Periféricos" <%= produto.categoria === "perifericos" ? "selected" : "" %>>Periféricos</option>
    </select>

    <!-- Campo Quantidade -->
    <label for="quantidade">Quantidade</label>
    <input type="number" id="quantidade" name="quantidade" value="<%= produto.quantidade %>" required>

    <!-- Botão de Enviar -->
    <button type="submit">Salvar Alterações</button>
  </form>
</body>
</html>
